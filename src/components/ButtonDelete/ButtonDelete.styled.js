import styled from 'styled-components';
import { TiDeleteOutline } from 'react-icons/ti';

export const ButtonWrapper = styled(TiDeleteOutline)`
  &:hover {
    transform: scale(1.2);
    cursor: pointer;
  }
`;
